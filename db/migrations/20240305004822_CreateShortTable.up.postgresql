create table short
(
    id   text    default gen_uid() not null
        constraint short_pk
            primary key,
    uses integer default 0         not null,
    url  text                      not null
);

